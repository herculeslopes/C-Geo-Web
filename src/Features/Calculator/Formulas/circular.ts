import { FormConfig } from "../Components/DynamicForm";

export const circularFormConfig: FormConfig = {
  fields: [
    { type: 'number', label: 'Diameter', name: 'diameter', unit: 'mm', value: 10 },
  ]
}