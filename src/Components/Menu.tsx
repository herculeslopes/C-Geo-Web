import styled from "styled-components";

const StyledMenu = styled.div`
  display: flex;
  width: 100px;
  background-color: #a8a8a8;
`

const Menu = () => {
  return <>
    <StyledMenu>

    </StyledMenu>
  </>
}

export default Menu;